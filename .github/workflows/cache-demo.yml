name: Cache Demo
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: prime-cache-cf7e7e5
        uses: actions/cache@v4
        with:
          key: cache-cf7e7e5
          path: node_modules
      
      - name: Install dependencies
        run: npm ci
      
      - name: Verify setup
        run: echo "✅ Cache demo complete!"
